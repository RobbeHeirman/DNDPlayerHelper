import React from 'react'
import ReactDOM from 'react-dom/client'
import Sheet from './character_sheet/sheet.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Sheet />
  </React.StrictMode>,
)
